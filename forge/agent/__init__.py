"""Interactive, model-driven agent mode (Claude-Code-style CLI).

This package turns Forge from a batch planner/executor into a conversational
coding agent: a single model drives tool calls turn-by-turn with the human in
the loop, with surgical edits, code navigation, permissioned writes, undo
checkpoints, and context compaction — layered on Forge's existing provider,
tool, and memory primitives.
"""
